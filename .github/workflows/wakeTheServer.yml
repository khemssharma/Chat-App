name: Keep Render API Awake

on:
  workflow_dispatch:         # lets you run manually from Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          curl -s https://chat-app-tgbj.onrender.com || true
